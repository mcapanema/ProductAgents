"""Knowledge services — the platform's public, agent-facing API (Layer 3).

Import services and their query/result types from ``productagents.knowledge``
(the top-level surface), not from these submodules.
"""
